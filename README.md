# Image_audio_classification
Processes, classifies and manipulates image and audio files.
**Images**: Negates, rotates, adjusts brightness, returns grayscale, pixelates and highlights edges. Uses KNN classifiers to predict label of a given image using labels of closest neighbrs to image. 
**Audio**: Reverses, slows down, speeds up, reverbs and clips audio files. 
